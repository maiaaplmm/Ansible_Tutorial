---

- hosts: all
  become: true
  tasks:

  - name: installa apache e php
    package:
      name:
        - "{{ apache_package }}"
        - "{{ php_package }}"
      state: latest
      update_cache: yes




